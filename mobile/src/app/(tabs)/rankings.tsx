import { ScrollView, StyleSheet, Text, View } from "react-native";
import { useGame } from "../../context/GameContext";
import { Card, Screen, SectionHeader } from "../../components/ui";
export default function RankingsScreen(){const {state}=useGame();const rows=state.countries.map(c=>({c,player:c.id===state.player.countryId?state.player.username:`COMMANDER OF ${c.code}`,power:c.id===state.player.countryId?state.player.power:c.power*1000000})).sort((a,b)=>b.power-a.power);return <Screen><ScrollView contentContainerStyle={{paddingBottom:30}}><SectionHeader title="RANKINGS"/>{rows.map((r,i)=><Card key={r.c.id} style={styles.row}><Text style={styles.pos}>#{i+1}</Text><View style={{flex:1}}><Text style={styles.name}>{r.player}</Text><Text style={styles.muted}>{r.c.name}</Text></View><Text style={styles.power}>{Math.round(r.power).toLocaleString()}</Text></Card>)}</ScrollView></Screen>}
const styles=StyleSheet.create({row:{flexDirection:"row",alignItems:"center",gap:10,paddingVertical:10},pos:{color:"#D7B36B",fontSize:12,fontWeight:"900",width:30},name:{color:"#E8DFCF",fontSize:10,fontWeight:"900"},muted:{color:"#707A80",fontSize:8,marginTop:2},power:{color:"#C7CED0",fontSize:9,fontWeight:"900"}});
