import { Alert, ScrollView, StyleSheet, Text, View } from "react-native";
import { useGame } from "../../context/GameContext";
import { Button, Card, Screen, SectionHeader } from "../../components/ui";
export default function IntelligenceScreen(){const {state,recruitAgent}=useGame();return <Screen><ScrollView contentContainerStyle={{paddingBottom:30}}><SectionHeader title="INTELLIGENCE"/><Card><Text style={styles.heading}>AGENT NETWORK</Text><Text style={styles.muted}>Deploy agents from an Intelligence Center. Missions are timestamp-based and persist through app restarts.</Text><Button title="RECRUIT AGENT" variant="gold" onPress={()=>{const p=state.provinces.find(x=>x.ownerId===state.player.countryId);if(p){const r=recruitAgent(p.id,"recon");Alert.alert(r.ok?"Agent deployed":"Blocked",r.message)}}}/></Card>{state.intelligence.length===0?<Card><Text style={styles.muted}>No active missions. Recon, sabotage and counter-intelligence can be queued from controlled provinces.</Text></Card>:state.intelligence.map(a=><Card key={a.id}><Text style={styles.name}>{a.name}</Text><Text style={styles.muted}>{a.mission.toUpperCase()} · {Math.round(a.successChance*100)}% success</Text></Card>)}</ScrollView></Screen>}
const styles=StyleSheet.create({heading:{color:"#C7B27E",fontSize:9,fontWeight:"900",letterSpacing:1},name:{color:"#E8DECB",fontSize:11,fontWeight:"900"},muted:{color:"#717B82",fontSize:9,lineHeight:14,marginVertical:8}});
